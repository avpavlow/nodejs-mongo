<div class="card">
    <div class="card-body">
        
        <h1>{{post.title}}</h1>
        <hr>    
        <small>Дата публикации: {{post.data}}</small><br>
        <hr>
        {{post.content}}
    </div>
</div>