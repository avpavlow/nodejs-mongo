<div class="card">
    <div class="card-body">
        
        <h1>{{post.titulo}}</h1>
        <hr>    
        <small>Дата публикации: {{post.data}}</small><br>
        <hr>
        {{post.conteudo}}
    </div>
</div>