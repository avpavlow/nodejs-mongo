{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h2>Редактировать категорию: </h2>
<div class="card">
    <div class="card-body">
        <form action="/admin/categories/edit" method="POST">
        <input type="hidden" name="id" value="{{category._doc._id}}" />
            <label for="name">Имя: </label>
                <input type="text" id="name" name="name" placeholder="Имя категории" class="form-control" value="{{category._doc.name}}">
            <label for="slug">Slug: </label>
            <input type="text" id="slug" name="slug" placeholder="Slug" class="form-control" value="{{category._doc.slug}}">
            <br>
            <button type="submit" class="btn btn-success">Редактировать категорию</button>
        </form>
    </div>
</div>