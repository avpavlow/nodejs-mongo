{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h2>Новая категория: </h2>
<div class="card">
    <div class="card-body">
        <form action="/admin/categories/nova" method="POST">
            <label for="name">Имя: </label>
                <input type="text" id="name" name="name" placeholder="Nome da category" class="form-control">
            <label for="slug">Slug: </label>
            <input type="text" id="slug" name="slug" placeholder="Slug da category" class="form-control">
            <br>
            <button type="submit" class="btn btn-success">Создать категорию</button>
        </form>
    </div>
</div>