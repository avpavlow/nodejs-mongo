<div class="jumbotron">
  <h1 class="display-4">Добро пожаловать в Блог на ноде</h1>
 <hr class="my-4">
  <p>
      Он использует служебные классы для типографики и интервалов для размещения содержимого внутри самого большого контейнера..</p>
  <a class="btn btn-primary btn-lg" href="#" role="button">Завести аккаунт</a>
</div>

<hr>
<h2>Недавние посты:</h2>

{{#each posts}}
<div class="card mt-4">
    <div class="card-body">
        
        
        <h3>{{title}}</h3>
        <p>{{descricao}}</p>
        <a href="/post/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
        <hr>
        <small>Категория: {{category.name}}</small>
        <small>Дата публикации поста: {{data}}</small>
        
    </div>

</div>
{{else}}
    <h5>Нет сообщений</h5>
{{/each}}